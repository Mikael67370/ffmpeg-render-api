{
  "name": "ffmpeg-render-api",
  "version": "2.0.0",
  "description": "Production FFmpeg rendering API for n8n video automation — Dockerized",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node --max-old-space-size=2048 server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "fs-extra": "^11.2.0",
    "uuid": "^9.0.1"
  }
}
